
const add = (f, s)=>{
    return f + s;
}

const multiply =(f, s)=>{
    return f * s;    
}

const divideByTwo = (f, s)=>{
    return  f/s
}

export {add, multiply, divideByTwo}

// export default add

// another way to export 
// const calculator = {add: add,};
// export default calculator